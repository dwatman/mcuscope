# hwbridge

Host side of the mcu-interface project: the `hwbridged` daemon and the `mcu` CLI.

See the repository root `README.md` for the quickstart and `docs/SPEC.md` for the
authoritative design contract.
